# Node.js dependencies
node_modules

# Example files directory (removed but kept in gitignore)
ejemplos

# Astro build output
.astro

# SQLite database files
*.db
*.sqlite
*.sqlite3
*.db-shm
*.db-wal

# User uploaded files (ignore contents but keep folder structure)
api/uploads/*
api/uploads/documents/*
api/uploads/images/*
